server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

      
logging:
  level:
    root: INFO
 
greeting:
  message: Hello And NAtalia
  
management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: '*'
